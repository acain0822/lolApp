$(function(){
  $('.schedule-carousel').flickity({
    // options
    cellAlign: 'left',
    contain: true,
    groupCells: 4,
    prevNextButtons: false,
    pageDots: false,
   
  });

 $('.standingheader').tooltip();
})
